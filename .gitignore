# The workspace_*_layout.json / workspace_*_programs.sh entries that used to be
# here were i3-resurrect session dumps. i3-resurrect is no longer installed on
# any machine and its save mode has been removed from the config, so nothing
# writes those files any more.

# Editor/OS noise
*.swp
*~
.DS_Store
